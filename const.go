package main

import (
	"fmt"
	// If we import library then must be used in prog otherwise it caused err.
	// "math"
)

func main() {
	const s string = "MyConstant";
	fmt.Println("string constant :", s);
}