package main

import "fmt"

func main() {
	// Declare variable
	var a = "initial"
	fmt.Println(a)
	// Declare and Initialize Variable at ones
	// It caused error with single quotation
	//f := 'Apple';
	f := "Apple"
	fmt.Println(f)
}
